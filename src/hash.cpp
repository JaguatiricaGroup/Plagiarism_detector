#include "hash.h"
#include <fstream>
#include <string>
#include <iostream>
#include <vector>
#include <math.h>
#include <stdlib.h>
/*using namespace std;
hash::hash()
{
  //ctor
}

hash::~hash()
{
  //dtor
}

/*hash::abrirTexto(string nomeArquivo){
  ifstream arquivo(nomeArquivo);
  string i= arquivo.c_str();
  cout << i;
}*/
/*string hash::arquivoToString(ifstream *arquivo){
  string str;
  //arquivo->open("leticia.txt");
  cout << arquivo->is_open()<< arquivo->good() << endl;
  if(arquivo->is_open() && arquivo->good()){
    *arquivo >> str;
    cout << str << endl;
    arquivo->close();
  }
  return str;
}

int* hash::strToInt(string *str){
  int *let=(int*)str->c_str();

//  for(int i=0;i<str->size();i++){
    //let[i]=str[i];
  //}
  return let;
}

*/
